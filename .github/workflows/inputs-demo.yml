name: Input demo
on: 
    workflow_dispatch:
        inputs:
            environment:
                description: choose enviorment to deploy
                required: true
                default: dev
jobs:
    inputs-demo:
        runs-on: ubuntu-latest
        steps:
            - name: deploy the app
              run: echo " deploying the application to ${{inputs.environment}}"
